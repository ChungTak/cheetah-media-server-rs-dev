//! Property-test scaffold for `cheetah-rtp-core`.
//!
//! Tests live under `tests/` and exercise RTP/RTCP framing, PS mux/demux, and
//! the Ehome protocol decoder.
//!
//! `cheetah-rtp-core` 的属性测试承载 crate。
//!
//! 测试位于 `tests/` 下，覆盖 RTP/RTCP 成帧、PS 复用/解复用以及 Ehome 协议解码器。
