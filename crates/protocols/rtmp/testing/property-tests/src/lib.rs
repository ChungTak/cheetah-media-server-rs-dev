//! `cheetah-rtmp-property-tests` provides property-based, round-trip, and capture-replay tests for `cheetah-rtmp-core`.
//!
//! `cheetah-rtmp-property-tests` 为 `cheetah-rtmp-core` 提供基于属性的测试、往返测试和抓包回放测试。

#![allow(clippy::missing_panics_doc)]
