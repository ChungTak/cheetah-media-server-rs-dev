//! Property-based tests for the HTTP-FLV protocol.
//!
//! Tests cover the shared `cheetah-codec` FLV egress mapping/bootstrap and the
//! `cheetah-http-flv-core` request parsing invariants.
//!
//! HTTP-FLV 协议属性测试。
//!
//! 测试覆盖 `cheetah-codec` 的 FLV 出口映射/引导以及 `cheetah-http-flv-core` 请求解析不变量。
