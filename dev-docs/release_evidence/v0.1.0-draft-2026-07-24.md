# Release Evidence Draft v0.1.0 (2026-07-24)

> Candidate: `main` / commit `087af4d` (MOD-05 merged)
> This is a draft release evidence package produced from local CI runs. Long-haul
> soak, interop fixtures and performance baselines are explicitly marked **BLOCKED**
> because they require real device/wire fixtures and a dedicated 24 h run that cannot
> be completed in a single Devin session.

## 1. 版本与制品

| Item | Value |
| --- | --- |
| git commit | `087af4d` (`main`) |
| Cargo.lock sha256 | `477eb25e7f718b73e5aa79b6f56ede060869896bb0eee0ea098ff57979cb85a2` |
| release profile build | verified via `cargo build --release -p cheetah-rtp-module` (no artifact uploaded) |

## 2. 代码门禁

| Check | Command | Result |
| --- | --- | --- |
| fmt | `cargo fmt --all -- --check` | PASS |
| clippy (rtp module) | `cargo clippy -p cheetah-rtp-module --tests -- -D warnings` | PASS |
| clippy (rtp core/driver) | `cargo clippy -p cheetah-rtp-core -p cheetah-rtp-driver-tokio --tests -- -D warnings` | PASS |
| unit tests (rtp-core) | `cargo test -p cheetah-rtp-core` | 77 passed |
| unit tests (rtp-driver-tokio) | `cargo test -p cheetah-rtp-driver-tokio` | 21 passed |
| unit tests (rtp-module) | `cargo test -p cheetah-rtp-module` | 53 passed (24 unit + 29 integration) |
| signal contracts | `cargo test -p cheetah-sdk --test signal_contracts` | 13 passed |
| dependency boundary | `cargo tree -p cheetah-rtp-module` reviewed: no `tokio` in `cheetah-rtp-core`, no `SIP/SDP/XML` crates | PASS |

## 3. 能力矩阵状态（摘要）

All P0–P4 implementation tasks from `dev-docs/plans-29-gb28181-impove` are reflected in
`01_audited_baseline_and_gap_register.md`. GB28181 signaling (SIP/SDP/XML) is not present
in the production crate tree.

| Capability | Status | Evidence |
| --- | --- | --- |
| RTP session lifecycle API | CODE PASS | `RtpSessionApi` typed request/result/generation/idempotency tests |
| PS demuxer | CODE PASS | `cheetah-codec` PS unit/property tests |
| RTP/RTCP core | CODE PASS | RTCP SR/RR/SDES/BYE, source binding, state machine tests |
| Driver Tokio | CODE PASS | UDP/TCP active+passive, half-close, port pool, load limits |
| GB28181 module | CODE PASS | typed REST adapter, no legacy SIP code, `gb28181_flow` integration tests |
| TALK/PLAY | CODE PASS | PCMA talkback, playback/download sender with `PlaybackApi` tests |
| EXT/RMV | CODE PASS | `RtpSessionApi` get/list/events; legacy SIP crates removed |
| COMPAT | CODE PASS | `Ehome2`/`Jtt1078` container mapping, profile gates |
| MOD file split | CODE PASS | `module.rs` → `http_service.rs` + `ingress.rs` + `module.rs`, all < 800 lines |
| SEC/OBS/OPS | CODE PASS | per-principal rate limit, mutation context validation, session lifecycle metrics, runbook, secret redaction in Debug output |

## 4. 未完成的 P5 证据

| Gate | Status | Blocker / Next command |
| --- | --- | --- |
| 24 h soak | BLOCKED | Requires dedicated runner and mixed RTP send/receive workload script. Command: `cargo test --release` + custom soak harness for 24 h. |
| Real device interop (ABL/ZLM/SMS/JTT/Ehome2) | BLOCKED | Requires sanitized fixtures/pcaps and authorized devices. Command: run `crates/protocols/rtp/testing/property-tests` and `gb-interop` lanes with real fixtures. |
| Performance baseline | BLOCKED | Needs identical hardware baseline for CPU/RSS/latency regression. Command: run benchmark suite and compare against previous release. |
| SBOM signed artifact | BLOCKED | Requires release build attestation and `cargo-sbom`/`cyclonedx` output signed by CI. Command: add `cargo sbom` step to release workflow. |
| mTLS/SAN/source identity contract (SEC-05) | CODE PASS | `AuthCredentials::mtls_identity` contract and adapter responsibilities documented in `docs/security/gb28181-rtp-mtls-source-identity.md`. |
| mTLS live certificate rotation (SEC-05) | BLOCKED | Needs certificate rotation harness in external network adapter. Command: add `sec-mtls-rotation` CI lane once adapter is implemented. |
| Secret redaction in logs/events/errors (SEC-06) | CODE PASS | `AuthCredentials` and `HttpRequest` `Debug` redact `authorization`/`deployment_token` and secret headers. |
| Automated secret scan (SEC-06) | BLOCKED | Needs `gitleaks` or `truffleHog` workflow wired into CI. Command: add `.github/workflows/secret-scan.yml`. |

## 5. 已知 Experimental 能力

- Ehome5, JTT1078 v2019 bidirectional egress, PS dynamic PSM fixtures remain `Experimental`.
- RTCP bidirectional SR/RR on real networks is `Experimental` pending interop evidence.
- TCP framing recovery fixture coverage is `Experimental` per `capability_matrix.md`.

## 6. 最终结论

**BLOCKED**

All code-level P0–P4 tasks are implemented and gated by tests. The release cannot be
marked `PASS` until the P5 long-haul soak, real-device interop, performance baseline,
SBOM, mTLS rotation and secret-scan evidence are produced and signed. The blockers and
next commands are listed in §4.

## 7. 签名

| Role | Conclusion | Name / Time | Evidence |
| --- | --- | --- | --- |
| architecture/API | BLOCKED | pending | PR review queue |
| codec/RTP | BLOCKED | pending | §2 test counts |
| GB/media API | BLOCKED | pending | §3 matrix |
| security | BLOCKED | pending | SEC-05/06 not run |
| performance/stability | BLOCKED | pending | soak/perf missing |
| release | BLOCKED | pending | §4 blockers |
